# atomfeeds-template

---

`atomfeeds-template` is a template of a personal site used to publish feeds(aka short blogs). This template is simple yet intuitive to easily navigate and read blogs, serve a simple *about* and *contacts* as a addon view.